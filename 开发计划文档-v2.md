# 多 Agent 桌宠开发文档 v2

> **文档状态（2026-07-29）**：历史设计文档。实现以 `README.md` / `进度记录.md` / 代码为准。
>
> | 类别 | 内容 |
> |------|------|
> | ✅ 已实现 | Phase 1–7：透明窗/托盘、Canvas 变换动画、**HTTP POST** `:4271/event`、Claude/Codex/Cursor hooks 适配器、单宠+会话过滤、换肤/右键菜单/配置面板、NSIS+自启+自动更新 |
> | ❌ 已废弃 / 勿照做 | WebSocket 传输；`cursor-extension/` VSCode 扩展方案（已改为 Cursor `hooks.json`）；Electron；Pull 目录监听；文末「待实现」过时 |
> | ⏳ 待做 / 未落地 | 多宠（每 session 一只）；Live2D/Spine；`trae`/`vscode` source；正式单测以外的大规模测试（已有最小单元/映射/Rust 测试）；设计 §12 中大量未实现的 Tauri commands |
>
> 修订版原文仍保留下方，便于对照演进。与 v1 的关键差异见 [§0 修订说明](#0-修订说明)。

---

## 0. 修订说明（相对 v1 的纠正）

| 议题 | v1 假设（已废弃） | v2 修正 |
|------|------------------|---------|
| 数据源 | 监听 `~/.cursor/hooks/` 目录里 Cursor 自动写入的 `agent_state_change` JSON | **该机制不存在**。Cursor Hooks 是"在事件发生时运行 shell 命令"，不吐状态 JSON，也不暴露 thinking/tool-use/permission 等内部状态 |
| 目标 Agent | 仅 Cursor | **多 Agent 通用**：Claude Code（真实 Hooks，粒度完美）为主，Cursor（VSCode 扩展粗粒度推断）、Codex CLI（stdout 解析）为适配器 |
| 通信模型 | Pull：桌宠轮询/监听目录 | **Push**：桌宠内置本地 HTTP POST server（`/event`），各 Agent 适配器推事件进来 |
| GUI 框架 | Electron + React（~80MB+ / 150MB+ 内存） | **Tauri + Svelte/React**（~3-10MB / ~30MB，常驻友好） |
| 动画 | 序列帧 `idle_01.png…` | **变换式程序动画**（基于现有正/左/右视图静态图，无需补帧）；后续可上 Live2D/Spine |

> ⚠️ v1 中 `src/core/hook-monitor.ts`、`config.json` 的 frame 清单、`pollInterval` 等基于废弃假设的内容，本版不再沿用。
>
> 🔧 **传输修正（v2.1 实施时）**：本文 §1/§2/§3/§8 中的 "WebSocket" 统一以 **HTTP POST**（`tiny_http`，`127.0.0.1:4271/event`）实现。原因：Claude Code Hooks 每次事件都新建进程，单向 fire-and-forget 推送用无状态 POST 更简单、更健壮（无需重连），且适配器可零依赖（curl 一行 / Node 内置 fetch）。

---

## 1. 技术栈

| 分类 | 方案 | 说明 |
|------|------|------|
| 桌面框架 | **Tauri 2.x** | Rust 后端 + 系统 WebView2（Win11 自带），体积/内存最优 |
| 前端 | **Svelte 5**（推荐）/ React | Svelte 编译产物小、动画响应快；React 亦可 |
| 后端语言 | Rust | 原生高效、内置本地 server 与事件分发 |
| 本地通信 | **HTTP POST**（`tiny_http`，localhost `/event`） | 各 Agent 适配器统一推事件；轮询弃用 |
| 动画 | **Canvas 2D + 变换** | 程序化 idle bob / breath / tilt / shake / jump，匹配现有素材 |
| 打包 | Tauri bundler + updater | Win NSIS，可选签名与自更新 |

---

## 2. 系统架构（Push 模型）

```
   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
   │ Claude Code  │  │   Cursor     │  │  Codex CLI   │
   │ (真实 Hooks) │  │ (VSCode 扩展) │  │ (stdout 解析)│
   └──────┬───────┘  └──────┬───────┘  └──────┬───────┘
          │ stdin JSON       │ 推断状态         │ 解析
          ▼                  ▼                  ▼
   ┌────────────────────────────────────────────────────┐
   │            适配器：归一化为统一事件协议              │
   │   { source, sessionId, state, tool?, detail?, ts }  │
   └───────────────────────┬────────────────────────────┘
                           │ WebSocket ws://127.0.0.1:PORT
                           ▼
   ┌────────────────────────────────────────────────────┐
   │              桌宠 (Tauri / Rust 后端)              │
   │  ┌──────────────┐  ┌──────────────┐  ┌─────────┐  │
   │  │ WS server    │→ │ 状态机       │→ │ emit →  │  │
   │  │ (归一/去重)   │  │ (优先级/聚合)│  │ 前端    │  │
   │  └──────────────┘  └──────────────┘  └────┬────┘  │
   └───────────────────────────────────────────┼─────────┘
                                               ▼
                              Canvas 变换动画引擎（Svelte 渲染层）
```

**核心改进**：数据采集与展示解耦。各 Agent 只需实现一个"把状态推到 `ws://127.0.0.1:PORT`"的轻量适配器，桌宠对它们一视同仁。

---

## 3. 统一事件协议（适配器 → 桌宠）

所有适配器向桌宠推送**同一 schema** 的 JSON（WebSocket 文本帧）：

```jsonc
{
  "source": "claude-code" | "cursor" | "codex" | "trae" | "vscode",
  "sessionId": "string",          // 用于多会话隔离
  "event": "state-change",        // 预留: "session-start" | "session-end"
  "state": "idle | thinking | tool-use | permission-prompt | ask-user | streaming | completed | error",
  "tool": "read_file",            // 可选
  "detail": "执行 read_file",     // 可选，人类可读
  "timestamp": 1700000000000
}
```

状态到动画的映射见 §5。

---

## 4. 数据源适配器设计（项目核心）

### 4.1 Claude Code 适配器（主，真实信号）

Claude Code 的 Hooks 是**真实、文档化**的，事件恰好覆盖目标状态：

| Claude Code Hook | stdin 事件字段 | → 宠物 state |
|------------------|---------------|-------------|
| `UserPromptSubmit` | `hook_event_name` 等 | `thinking` |
| `PreToolUse` / `PostToolUse` | `tool_name`,`tool_input` | `tool-use` |
| `Notification`（需权限/输入） | `message` | `permission-prompt` / `ask-user` |
| `Stop` / `SubagentStop` | — | `completed` |
| `SessionStart` | — | `idle` |
| 工具非零退出 | — | `error` |

适配器 = 一个极小 CLI（Rust 或 Node），读 stdin JSON → 映射 → 推 WebSocket。在 `~/.claude/settings.json` 注册：

```jsonc
{
  "hooks": {
    "PreToolUse":  [{ "hooks": [{ "type": "command", "command": "pet-bridge --state tool-use" }] }],
    "PostToolUse": [{ "hooks": [{ "type": "command", "command": "pet-bridge --state tool-use" }] }],
    "Notification":[{ "hooks": [{ "type": "command", "command": "pet-bridge --state permission-prompt" }] }],
    "Stop":        [{ "hooks": [{ "type": "command", "command": "pet-bridge --state completed" }] }],
    "UserPromptSubmit":[{ "hooks": [{ "type": "command", "command": "pet-bridge --state thinking" }] }]
  }
}
```

`pet-bridge` 读 stdin、注入 `sessionId`、推 `ws://127.0.0.1:PORT`。端到端真实可用。

### 4.2 Cursor 适配器（粗粒度，诚实降级）

Cursor 内部 Agent 状态是专有的，不暴露。通过一个 **VSCode 扩展**推断：

| 信号 | 推断 state | 局限 |
|------|-----------|------|
| `onDidChangeTextDocument` / `onDidSaveTextDocument` | `tool-use` | 仅"在工作"，非真实工具调用 |
| `terminal` 启动/退出 | `tool-use` / `completed` | — |
| `diagnostics` 出现 error | `error` | 误报可能 |
| 一段时间无活动 | `idle` | — |

**无法可靠获取** `thinking` / `streaming` / `permission-prompt`。文档明确该限制，UI 上以"工作/空闲/出错"三态呈现，不假装有细粒度。这是诚实权衡，避免过度承诺。

### 4.3 Codex CLI 适配器

Codex 是终端 Agent。用一个 **wrapper 脚本**包裹 `codex` 调用，解析 stdout 流中的标记行 → 推事件。粒度介于 Cursor 与 Claude Code 之间。

### 4.4 适配器接口约定

每个适配器目录独立、可单独分发：

```
adapters/
├── claude-code/        # pet-bridge CLI + settings.json 示例
├── cursor-extension/  # VSCode 扩展源码
└── codex-cli/          # wrapper 脚本
```

---

## 5. 状态映射表（修订）

| 统一 state | 动画 | 文案 | 优先级 | 真实可获取性 |
|-----------|------|------|--------|-------------|
| `idle` | 待机浮动 | 闲置中… | 0 | ✅ 全部 |
| `thinking` | 轻微倾斜 | 思考中 🤔 | 2 | ✅ Claude Code；⚠️ Cursor 无 |
| `tool-use` | 抖动/忙碌 | 执行 🔧 | 3 | ✅ 全部（Cursor 为推断） |
| `permission-prompt` | 等待弹动 | 等待审批 ✋ | 3 | ✅ Claude Code；⚠️ Cursor 无 |
| `ask-user` | 等待弹动 | 需你确认 💬 | 3 | ✅ Claude Code |
| `streaming` | 打字感 | 输出中 📝 | 2 | ✅ Claude Code/Codex |
| `completed` | 跳跃一次 | 完成 ✅ | 1 | ✅ 全部 |
| `error` | 抖动一次 | 出错 ❌ | 3 | ✅ 全部 |

多会话聚合沿用 v1 的"取最高优先级"策略。

---

## 6. 动画方案：变换式程序动画

现有素材为**角色的正/左/右视图静态图**，无序列帧。用 Canvas 对单图施加参数化变换模拟状态，零补帧成本：

```jsonc
// assets/config/animations.json
{
  "idle":    { "view": "front", "bob":   { "amp": 4, "period": 1200 }, "breath": { "sy": 0.02, "period": 1500 } },
  "thinking":{ "view": "front", "tilt":  { "amp": 6, "period": 700 },  "dots": true },
  "tool-use":{ "view": "front", "shake": { "amp": 2, "period": 90 } },
  "waiting": { "view": "front", "bounce":{ "amp": 6, "period": 1000 } },
  "streaming":{ "view": "front", "bob":  { "amp": 2, "period": 250 }, "twitch": true },
  "success": { "view": "front", "jump":  { "h": 28 }, "once": true, "next": "idle" },
  "error":   { "view": "front", "shake": { "amp": 8, "period": 45 }, "once": true, "duration": 1200, "next": "idle" },
  "walk-left":{ "view": "left",  "slide": { "amp": 0, "period": 0 } },
  "walk-right":{ "view": "right","slide": { "amp": 0, "period": 0 } }
}
```

引擎按状态查表，对当前 `view` 图片施加 `translate/scale/rotate`（正弦驱动），`requestAnimationFrame` 循环。后续若获得骨骼素材，可平滑升级到 Live2D/Spine 而不影响状态机。

---

## 7. 目录结构

```
codex-pet/
├── src-tauri/                 # Rust 后端
│   ├── src/
│   │   ├── main.rs
│   │   ├── server.rs           # 本地 WebSocket server
│   │   ├── state_machine.rs    # 状态机/聚合/去重
│   │   └── commands.rs         # 暴露给前端的 Tauri command
│   ├── Cargo.toml
│   └── tauri.conf.json
├── src/                        # 前端 (Svelte)
│   ├── App.svelte
│   ├── lib/
│   │   ├── renderer.ts         # Canvas 变换动画引擎
│   │   ├── state-machine.ts
│   │   └── types.ts
│   └── assets/
│       ├── character/          # 正视图.png / 左视图.png / 右视图.png
│       └── config/animations.json
├── adapters/
│   ├── claude-code/
│   ├── cursor-extension/
│   └── codex-cli/
├── package.json
└── README.md
```

---

## 8. 关键代码参考

### 8.1 Tauri 本地 WebSocket server（Rust）

```rust
// src-tauri/src/server.rs
use tauri::{AppHandle, Emitter};
use tokio::net::TcpListener;
use tokio_tungstenite::accept_async;
use futures_util::StreamExt;

pub async fn start(app: AppHandle, port: u16) {
    let listener = TcpListener::bind(("127.0.0.1", port)).await.unwrap();
    while let Ok((stream, _)) = listener.accept().await {
        let app = app.clone();
        tokio::spawn(async move {
            let mut ws = accept_async(stream).await.unwrap();
            while let Some(Ok(msg)) = ws.next().await {
                if let Ok(text) = msg.into_text() {
                    // 归一/去重/聚合后转发给前端
                    if let Some(state) = state_machine::handle_event(&text) {
                        let _ = app.emit("pet-state", state);  // → 前端
                    }
                }
            }
        });
    }
}
```

`state_machine::handle_event` 做幂等去重（同 `sessionId` 同 state 不重发）与多会话聚合，输出最终渲染状态。

### 8.2 Claude Code pet-bridge 适配器（Node，~30 行）

```js
// adapters/claude-code/pet-bridge.mjs
import WebSocket from "ws";
const ws = new WebSocket("ws://127.0.0.1:4271");

let stdin = "";
for await (const chunk of process.stdin) stdin += chunk;
const data = JSON.parse(stdin || "{}");

const state = process.argv.includes("--state")
  ? process.argv[process.argv.indexOf("--state") + 1]
  : "idle";

ws.on("open", () => {
  ws.send(JSON.stringify({
    source: "claude-code",
    sessionId: data.session_id ?? "default",
    event: "state-change",
    state,
    tool: data.tool_name,
    detail: data.tool_name ? `执行 ${data.tool_name}` : undefined,
    timestamp: Date.now(),
  }));
  ws.close();
  process.exit(0);
});
```

### 8.3 Canvas 变换动画引擎（前端 TS，骨架）

```ts
// src/lib/renderer.ts
const ANIM: Record<string, AnimDef> = /* load animations.json */;
let state = "idle", t0 = 0, onceStart = 0;

export function setState(s: string) { state = s; t0 = performance.now(); if (ANIM[s].once) onceStart = t0; }

export function loop(ctx: CanvasRenderingContext2D, imgs: Record<string, HTMLImageElement>, now: number) {
  const def = ANIM[state] ?? ANIM.idle;
  const img = imgs[def.view];
  const t = (now - t0) / 1000;
  let dy = 0, rot = 0, sy = 1;
  if (def.bob)    dy   += Math.sin(t * 2 * Math.PI / def.bob.period * 1000) * def.bob.amp;
  if (def.tilt)   rot  += Math.sin(t * 2 * Math.PI / def.tilt.period * 1000) * (def.tilt.amp * Math.PI / 180);
  if (def.breath) sy   += Math.sin(t * 2 * Math.PI / def.breath.period * 1000) * def.breath.sy;
  if (def.shake)  dy   += Math.sin(t * 2 * Math.PI / def.shake.period * 1000) * def.shake.amp;
  // once 类（success/error）播完回 idle
  if (def.once && now - onceStart > (def.duration ?? 800)) setState(def.next ?? "idle");
  ctx.clearRect(0, 0, ctx.canvas.width, ctx.canvas.height);
  ctx.save();
  ctx.translate(ctx.canvas.width / 2, ctx.canvas.height / 2 + dy);
  ctx.rotate(rot); ctx.scale(1, sy);
  ctx.drawImage(img, -img.width / 2, -img.height / 2);
  ctx.restore();
}
```

---

## 9. 开发阶段（修订）

| Phase | 内容 | 预估 |
|-------|------|------|
| 1 | Tauri 透明置顶窗口 + 托盘 + 拖拽/位置记忆 | 1-2 天 |
| 2 | Canvas 变换动画引擎 + 状态机 + **模拟事件**联调 | 2 天 |
| 3 | Rust 本地 WebSocket server + 前后端事件通道 | 1-2 天 |
| 4 | **Claude Code 适配器** + 端到端真实打通（里程碑） | 2 天 |
| 5 | Cursor 扩展适配器 + Codex CLI 适配器 | 2-3 天 |
| 6 | 多宠/气泡/右键菜单/配置面板 | 2-3 天 |
| 7 | Win 打包 + 开机自启 + 可选更新 | 1-2 天 |

里程碑：Phase 4 结束即有"真实 Agent 驱动桌宠"的可用 Demo。

---

## 10. 与 v1 的取舍清单

- ❌ 删除：`~/.cursor/hooks/` 文件监听、序列帧播放器、`pollInterval` 轮询、Named Pipe。
- ✅ 保留并强化：状态优先级聚合、多 Session 隔离、气泡/卡片、托盘、打包。
- 🔁 改造：数据源从"Pull 目录"改为"Push WebSocket"；GUI 从 Electron 改为 Tauri；动画从"帧序列"改为"变换"。

## 11. 数据结构

```typescript
// 统一事件（适配器 -> 桌宠 WebSocket）
interface AgentEvent {
  source: 'claude-code' | 'cursor' | 'codex' | 'trae' | 'vscode';
  sessionId: string;
  event: 'state-change' | 'session-start' | 'session-end';
  state: AgentState;
  tool?: string;
  detail?: string;
  timestamp: number;
}

type AgentState =
  | 'idle' | 'thinking' | 'tool-use' | 'permission-prompt'
  | 'ask-user' | 'streaming' | 'completed' | 'error';

// 宠物实例（每个 sessionId 一只）
interface PetInstance {
  id: string;                  // `${source}:${sessionId}`
  source: AgentEvent['source'];
  sessionId: string;
  name: string;
  currentState: AgentState;
  previousState: AgentState;
  stateHistory: StateRecord[]; // 最近 10 条
  position: { x: number; y: number };
  isActive: boolean;
  lastActivity: number;
}
interface StateRecord { state: AgentState; timestamp: number; detail?: string; }
```

`config.json`（v2）：

```jsonc
{
  "pet": { "name": "Codex Pet", "scale": 1.0, "position": { "x": 100, "y": 100 }, "alwaysOnTop": true, "clickThrough": false },
  "server": { "host": "127.0.0.1", "port": 4271 },
  "animation": { "configPath": "assets/config/animations.json" },
  "multiPet": { "enabled": false, "layout": "row" },
  "debug": false
}
```

## 12. API 设计

**Rust 侧（Tauri commands，前端通过 `invoke` 调用）：**

| Command | 作用 |
|---------|------|
| `get_state()` | 返回当前 `PetSnapshot`（所有 pet + 聚合状态） |
| `set_position(x, y)` | 移动窗口并持久化 |
| `reset_position()` | 回到默认/屏幕右下 |
| `toggle_always_on_top()` | 切换置顶 |
| `toggle_visibility()` | 显示/隐藏 |
| `get_config()` / `set_config(partial)` | 读写配置 |

**Rust -> 前端事件（`app.emit`）：** `pet-state`、`pet-created`、`pet-updated`、`pet-inactive`、`global-state-change`。

**前端（Svelte）API：** 一个 `petStore` 订阅 `pet-state` 事件 -> 调 `renderer.setState(state)` 驱动 Canvas；组件层负责气泡、卡片、右键菜单。

## 13. 打包配置（tauri.conf.json 关键项）

```jsonc
{
  "productName": "Codex Pet",
  "app": {
    "windows": [{
      "title": "Codex Pet",
      "width": 200, "height": 200,
      "decorations": false,
      "transparent": true,
      "alwaysOnTop": true,
      "resizable": false,
      "skipTaskbar": true
    }],
    "trayIcon": { "iconPath": "icons/tray.png", "tooltip": "Codex Pet" }
  },
  "bundle": {
    "active": true,
    "targets": ["nsis"],
    "icon": ["icons/icon.ico"],
    "windows": { "webviewInstallMode": { "type": "downloadBootstrapper" } }
  }
}
```

## 14. 测试策略

- **单元**：状态机优先级聚合、同会话去重、`AgentEvent` schema 校验（Rust `#[cfg(test)]`）。
- **适配器**：用 fixture stdin 验证 Claude Code 映射（`UserPromptSubmit->thinking`、`PreToolUse->tool-use` 等）。
- **集成**：内置 `mock-adapter` CLI 周期推送随机事件，端到端验证动画状态机切换。
- **手动验收**：真实 Claude Code 跑一次任务，观察桌宠 idle->thinking->tool-use->completed 流转。

## 15. 里程碑与验收标准

| 里程碑 | 包含 Phase | 验收 |
|--------|-----------|------|
| M1 | 1-2 | 透明置顶窗口 + 变换动画，mock 事件驱动状态切换 |
| M2 | 3-4 | 真实 Claude Code 端到端驱动桌宠 |
| M3 | 5 | Cursor / Codex 适配器接入 |
| M4 | 6-7 | 多宠/配置面板/打包发布 |

---

**文档版本** v2.2 · 2026-07-29 · 状态：**Phase 1–7 已落地**；本文作历史设计参考，文首表格为准
