# 项目进度记录

> 最近更新：2026-07-31

## 当前状态：v0.3.4（品牌图标 + 会话监听体验）

## 工程（2026-07-31 · P5 日常可信度 + 可发现性）

- **静默提示**：idle 且已引导/已装 hooks 时，连续 `silenceHintMs`（默认 30min）无事件 → 气泡「好像没接到 Agent」；点开设置；`connectionHints` 可关。
- **启动自检**：`hooks_status` 存在 `needsUpdate` → 优先提示「连接需补装」。
- **托盘**：菜单指纹未变跳过重建；有变 400ms 合并。
- **渲染**：按素材 fps / 12fps 跳帧；页面隐藏停 rAF。
- **验证**：`tsc` / 前端单测（含 connection-hints）/ Rust settings 缺字段兼容。

## 修复（2026-07-30 · 完成被盖住 + 思考循环跳变）

- **完成卡住**：全部模式下其它会话的 streaming/thinking（优先级更高）会盖住刚完成的对话；改为**优先展示最近有事件的会话**。
- **Cursor**：`afterAgentResponse` 改为 `completed`（整条回复结束；勿再当「整理回复」思考，否则 stop 不来就一直卡工作态）。
- **思考循环**：中段 `loopPingpong: true`，往返播放代替首尾硬切（缝 MSE~4900）。

## 体验（2026-07-30 · 动画切换与终态续聊）

- **工作态动画**：thinking/tool-use/reading/streaming 互相切时 defer 到**循环边界**，只保留最新目标；等待/完成/出错仍立即切（可先 outro）。
- **终态续聊**：完成态不再把「思考中」当噪声挡住（新一轮 UserPromptSubmit）；仍挡「整理回复」等收尾，无需先点消散才能进下一对话。

## 素材（2026-07-30 · 两套「读」动作）

- **`读.mp4` → streaming**：`--full --keep 5`，61 帧 pingpong（读书+漂浮气泡）。
- **`read.mp4` → reading**（新动画）：同参数，笔记本+铅笔；`animFor`：Read/Grep/Glob 等 tool-use 切 reading，其余仍 tool-use。
- skins / AnimName / WORKING_ANIMS 已纳入 reading。
- **重切**：用户换源后 `read.mp4` 再抽一版（翻书+文档图标，61 帧 pingpong），替换旧「笔记本+脚边铅笔」表。

## 素材（2026-07-30 · thinking 整段动作）

- **根因**：默认循环段检测只截了 ~11 帧托腮小环，丢掉举手等完整过程。
- **改**：管线加 `--full`（整段保留；首尾不似则 pingpong 由前端往返）；顺带修正 pingpong 失败路径不再把逆序烤进表。
- **重切（三段式）**：`思考.mp4 --intro 12 --outro 11 --keep 3` → **61 帧**（入场举手 12 → 托腮循环 38 → 出场灯泡 11）。验收 `_preview-thinking.png`。

## 工程（2026-07-30 · P4 稳定向开工）

- **预览图**：`skins.ts` glob 排除 `_preview*.png`，不再打进前端包。
- **hooks 健康**：配置中 `pet-bridge` 路径文件不存在 → 黄灯 +「补装」；优先于缺事件提示。
- **发版**：`scripts/check-release-assets.mjs` + `release.yml` 校验 `.exe`/`.sig`/`latest.json`；README 补 draft 说明与安装版冒烟清单。
- **目标**：合入后 bump **0.3.2**（不发新皮肤）。

## 修复（2026-07-30 · 设置里安装 Codex hooks 报 EISDIR）

- **根因**：`adapters_dir` 经 `canonicalize` 得到 `\\?\D:\...`，传给 `node` 入口脚本时 Node `realpath` 对 `D:` 抛 `EISDIR`。
- **改**：`hooks_cmd::for_node_path` 剥掉 `\\?\` / `\\?\UNC\` 后再启进程；Codex 安装器修正 `[features] hooks=true` 检测正则，避免重复追加。

## 优化（2026-07-30 · 体验与适配器）

- **hooks 缺项检测**：`hooks_status` 标 `needsUpdate`（Cursor 缺 `beforeShellExecution` 等）；配置面板黄灯 +「补装」。
- **去重**：Rust `route` 指纹改为 `state|tool|detail`，同态换工具会转发；过期会话同步清 `last`。
- **等待/终态**：仅挡住空转 thinking（「思考中」等）；`afterShell`→tool-use 可离开门闩，避免审批动画卡到结束。

## 修复（2026-07-30 · Cursor 点 Run 仍显示思考中）

- **根因**：本机 hooks 未含 P3 的 `beforeShellExecution`（点 Run 门闩事件）；停在 `afterAgentThought`→thinking。
- **改**：安装器补 `afterShell/MCPExecution`；`preToolUse(Shell/MCP)`→等待审批；`permission-prompt` 优先级 4；请对 Cursor **再点一次安装**。

## 修复（2026-07-30 · 默认落位偏低 + 气泡压角色）

- **落位**：用显示器 `work_area`（排除任务栏）+ DPI 换算物理尺寸；启动时钳已存坐标进工作区。「重置位置」同样生效。
- **气泡**：窗口增高上下留白（顶 44 / 底 24），气泡与会话条改文档流，不再绝对叠在 canvas 上。

## 当前状态：v0.3.2（体验/素材/P4 稳定项；已发版）

## 新功能（2026-07-30 · P3 适配器校准与文案/皮肤）

- **MAP 校准**：Codex 安装补 `SessionEnd/PreCompact/PostCompact/Subagent*`；Cursor 补 `afterAgentResponse/afterFileEdit/beforeShell|MCPExecution`。**无一映射 streaming**（测断言）。Cursor 审批仅观察 shell/MCP，不能代点批准。
- **诚实标注**：配置「Agent 连接」+ README；streaming 保留素材与规则文案；`PET_DEBUG=1` 仍用于真机载荷。
- **文案**：工具别名（Bash→终端、Grep→搜索…）；`copyAiTerminalOnly`；配置展示上次 API 错误（`copy-error` 事件）。
- **皮肤注册表**：`src/assets/skins.json`；`src/lib/skins.ts` + Rust `include_str!` 菜单同源；Config/renderer 不再三处手写。
- **验证**：`tsc` / 前端 + 适配器 + Rust 全绿。

## 当前状态：P2 交互与会话已完成；下一阶段 P3（适配器校准 / 文案 / skins.json）

## 新功能（2026-07-30 · P2 交互与会话）

- **缩放**：`petScale` 0.75/1/1.4 → 窗口 150/200/280；canvas/精灵/气泡同步；配置档位热更。
- **重置位置**：托盘/右键「重置位置」+ 配置按钮 → 主屏右下（随当前尺寸）。
- **终态提醒**：`terminalNotify`（默认开）→ `request_user_attention` 任务栏闪烁；可关。
- **会话轻列表**：配置「监听会话」`list_sessions` / `set_session_filter` / `get_session_filter`，3s 刷新；与托盘过滤同步。
- **衰减边角**：过滤态 `toIdle` 只清锁定会话；`setFilter` 保留目标最近状态；单测覆盖。
- **验证**：`tsc` / 前端 + 适配器 + Rust（含 `pet_logical_size`）全绿。

## 当前状态：P1 冷启动已完成；下一阶段 P2（缩放/通知/会话列表）

## 新功能（2026-07-30 · P1 冷启动与默认可视）

- **Agent 连接**：配置面板一键安装/卸载 Claude/Codex/Cursor hooks；`hooks_status` / `probe_event_channel`；`adapters` 打入 `bundle.resources`。
- **默认皮肤**：`Settings::default.skin = green-anim`（已有 settings.json 不覆盖）；`onboardingDone` 首次三句提示。
- **可读性**：气泡两行 13px；会话条「全部·N / 锁定」；过滤态蓝底。
- **验证**：`tsc` / 前端 40 + 适配器 22 + Rust 7 全绿。

## 当前状态：P0 工程闸门已完成；下一阶段 P1（hooks 面板 / 默认皮肤）

## 工程（2026-07-30 · P0 工程闸门）

- **CI**：新增 `.github/workflows/ci.yml`（tsc + npm test + check-versions）；`release.yml` 在 tauri-action 前跑同一套。
- **版本**：`scripts/check-versions.mjs` / `bump-version.mjs`；修正 `package-lock.json` 顶层 0.1.1→0.2.0。
- **契约测**：`pet-bridge` 不可达端口仍 exit(0)<1.5s；三源 `install-hooks` 临时 HOME 装/幂等/卸；`working-churn` 纯函数单测。
- HTTP `/event` 全链路冒烟依赖 Tauri AppHandle，P0 仍靠既有 `EventRouter` 单测；完整 server 冒烟留待后续。
- **验证**：`check-versions` / `tsc` / 前端 40 + 适配器 22 + Rust 7 全绿。

## 当前状态：v0.2.0 + 智能文案旁路（规则/OpenAI，默认关）

## 新功能（2026-07-30 · 气泡文案人格化 + 终态摘要）

- **旁路可降级**：`src/lib/copy/`（EventRing + RuleProvider + OpenAICompat + CopyQueue）；失败/关闭回退 `detail || label`；不进 pet-bridge。
- **规则模板**：零网络；tool-use →「翻翻看「Grep」…」；终态按本轮工具轨迹摘要。
- **OpenAI 兼容**：Rust `chat_complete`（reqwest，4s 超时，Key 不写日志）；设置里填 Base URL / Key / Model。
- **设置**：`copyEnabled` 默认 false；配置面板「智能文案」节 +「试写一句」；旧 settings.json 缺字段 serde 兼容。
- **验证**：`tsc` / 前端 34 + 适配器 16 / Rust 7（含 Settings 缺字段兼容）全绿。

## 修复（2026-07-30 · 开发态设置窗 ERR_EMPTY_RESPONSE）

- Vite `host:false` 在部分 Windows 上只听 `[::1]`，WebView 走 `127.0.0.1` → 空响应。
- 改：`vite` 显式绑 `127.0.0.1`，`devUrl` 对齐；打开设置时 `reload` 避免卡在启动失败页。

## 历史状态：v0.2.0 已发布（帧动画皮肤 green-anim 七状态正式上线）

## 发版（2026-07-30 · v0.2.0）

- 素材与功能提交 `c6a1c98`（44 文件：管线/播放器/7 状态素材与精灵表/文档），版本 bump `f3c6f24`（三处同步 0.1.1→0.2.0）。
- `git tag v0.2.0` → CI release ~7min → draft（setup.exe + .sig + latest.json 齐全）→ `gh release edit --draft=false` 发布。
- 已安装实例（v0.1.1）下次启动经 latest.json 静默自动更新到 v0.2.0——自动更新闭环第二次实战验证。
- **排障**：用户反馈"重启后没更新"——实为重启发生在 CI 发布前的 draft 窗口（`releases/latest` 仍指 v0.1.1）。发布完成后实测启动 v0.1.1：check(0.7s) → 下载（~180KB/s) → 静默安装 → relaunch，进程版本变为 0.2.0，链路正常。教训：draft 期间 `releases/latest` 不指向新版本，客户端检查只会发生在下次启动。

## 整理（2026-07-29 · 素材与文件清理）

- **删除**：`data/_debug/`（排查用拼条）、`data/read.mp4`（旧版 720×960 输出中素材，streaming 实际取自 `读.mp4`）、`data/Character Vedio/角色帧动画生成.mp4`（v1 带水印花屏闲置素材，idle 已改用 `闲置.mp4`）、精灵表目录的裸 `_preview.png`（管线每次运行覆盖的临时验收图）。
- **保留**：7 个状态源视频（重切精灵表的原料）、`Character Image/`（角色设计图）、按状态命名的 `_preview-<状态>.png` 验收图（补齐 waiting）。
- **文档同步**：README 序列帧描述更新为最终设计（7 状态专属序列/三段式/once/pingpong）+ 目录树补 frame-player 与 assets/frames；视频需求.md 修正 waiting 文件名、补 `--keep` 调参说明。
- 验证：tsc / 23 前端 + 适配器测试 / 5 Rust 测试 / build 全绿。

## 新功能（2026-07-29 · 等待审批 waiting 序列接入）

- **素材**：`data/等待审批.mp4`（5.02s/24fps，沙漏+时钟道具出现、合掌等待）。至此 7 个状态视频全部制作完毕并接入。
- **管线**：`--fps 10 --intro 12 --outro -1` → 73 帧三段式（入场 12 帧道具出现+合掌 → 循环 [12,61) 49 帧合掌等待+时钟走时 → 出场 12 帧倒放=道具消退+垂手）。613KB。
- **验证**：循环段逐帧道具区域扫描无缺失（沙漏/时钟与头发连通，keep_main_component 未误杀）；接缝 60→12 MSE 1874 属硬切，但循环本体近乎静止（相邻帧 MSE 33），接缝仅表现为时钟指针小跳，可接受。
- **接线**：renderer.ts FRAME_SKINS 加 waiting（permission-prompt/ask-user 本就映射到 waiting 动画名，状态机零改动）。waiting 属非工作态：工作态→waiting 播齿轮消退、waiting→工作态播道具消退，生命周期闭环。
- **注意**：管线每次运行会覆盖 `_poster.png`，已从 idle 首帧还原（配置面板缩略图固定用闲置姿势）。

## 修复记录（2026-07-29 · success "顺序不对"= ✓ 中途消失）

- **现象**：success 动画中绿色 ✓ 在约 1s 处弹出后消失，最终定格帧没有 ✓——用户感知为"素材顺序不对"（✓ 理应出现并保持到结尾）。
- **根因**：`keep_main_component` 只保留最大连通域（杀豆包水印的兜底策略），而 ✓/星星是漂浮在头顶的**独立连通域**，从第 13 帧（✓ 与头发不接触）起被整帧删除。源视频本身顺序正常（蓄力→跳跃→✓欢呼定格）。
- **修复**：管线新增 `--keep N`（保留面积前 N 的连通域，默认 1=原行为）。success 重切：`--start 2.2 --once --keep 5`（成功结束.mp4 右下角确认无水印，可安全放开）。验证：全部 34 帧头顶区域不透明像素 >0，✓+星星保持到定格帧；格 12/33 与源帧 77/119 逐一比对一致。248KB。
- **附带发现**：① 源视频庆祝段有轻微镜头推近（AI 相机漂移，末段小腿出画）——素材固有，当前可接受，重拍需注意"固定镜头"；② 管线改为**仅 `--name idle` 时才写 `_poster.png`**（此前每次运行都覆盖缩略图）。
- **排查弯路备忘**：缩略图拼条的肉眼判读两次误导（"✓ 在底部"、"画面缩放矛盾"）；以数值（区域 alpha/深色跨度）和原尺寸单格对比为准。OpenCV 顺序解码与按帧 seek 在本素材上结果一致（实测 bit 级相同），VFR 不影响抽帧。

## 修复记录（2026-07-29 · 齿轮循环"特效消失"）

- **现象**：用户观察到齿轮循环中"人物和特效消失"。
- **排查**：精灵表循环段 20-46 帧逐帧目检无缺帧；屏幕连拍 10s（100 帧）静态循环无任何空白帧——纯循环无 bug。真实原因是**工作态震荡**：真实会话中 thinking ↔ tool-use 高频交替（每次工具调用后都有 thinking 事件），每次切换都触发 1s 出场（齿轮消退）+ 1s 入场（齿轮重现）→ 用户感知为"循环中特效反复消失"。
- **修复（工作态迟滞）**：`WORKING_ANIMS = {thinking, tool-use, streaming}`；工作态**之间**切换跳过出场（不播齿轮消退），切入三段式序列时**跳过入场**（animStart 预减 introDurationMs 直接进循环段）；出场只在工作真正结束（切到 idle/success/error/waiting 等非工作态）时播放。
- **附注**：success 结尾"还不太对"用户暂缓处理；屏幕连拍排查法受前台窗口遮挡干扰（用户切到 IDE 后拍的是编辑器），后续排查需先确认桌宠窗口可见。

## 修复记录（2026-07-29 · 帧动画首测两问题）

- **问题一：齿轮特效循环出现/消退（应为保持旋转、结束时才消退）**
  - 分析：源视频齿轮从不消失（满特效到片尾），"消退"在素材里不存在；用户期望 = 入场（出现）→ 循环（旋转）→ 出场（消退）。
  - 方案：**三段式播放**。管线 `--intro N --outro M`，`--outro -1` = **入场帧倒放作出场**（出现倒放=消失，零额外素材）。tool-use 重切：`--start 1.2 --end 5.02 --fps 10 --intro 12 --loop-start 20 --outro -1`（入场 12 帧举手+齿轮出现 → 循环 [20,47) 27 帧满特效旋转——循环段前 8 帧按反馈踢除 → 出场 12 帧倒放消退）。
  - 前端：`frame-player.ts` 新增 `phasedIndex`（含 loopStart）/`outroIndex`/`outroDurationMs`（纯函数 + 单测）；`renderer.setAnim` 切换时若当前序列有出场段 → 先播出场（`drawOutro`）再切目标动画；出场期间同动画事件取消出场回循环段。
- **success 动作不全 + 不应循环**：循环段检测只取了末尾定格（41..59），丢了蓄力→举手过程。改 **once 一次性播放**：`--start 2.2 --once` 取 34 帧完整动作（蓄力→比耶→欢呼+星星），播完**停在最后一帧**（终态本就保持到点击确认）。管线加 `--once`，`frameIndex` 支持 once 钳制。
- **问题二：齿轮状态右下角空白遮挡（角色显示不全）**
  - 根因：`remove_white_bg` 的**水印遮角**无条件把右下角 32%×9% 填白删掉；新视频无水印，角色腿/齿轮伸入该区域被误删。
  - 修复：`wm_frac` 参数化，视频管线传 `None` 不遮角（水印本就由最大连通域过滤兜底）；cutout.py CLI 行为不变。全部精灵表重切。
- **顺带**：① 新视频 `读.mp4`（读书+漂浮文字）接入为 **streaming** 状态。② Vite watch 忽略 `data/`、`tools/`——此前 dev 服务器因 watch 正在写入的 mp4 报 EBUSY 崩溃。
- 验证：`tsc`/`npm test`（前端 23 + Rust 5 全绿）/`build` 通过；6 张精灵表共 ~2.4MB；预览确认右下角角色完整、齿轮三段齐整、success 动作完整。

## 历史状态：帧动画皮肤 green-anim 五状态落地（视频抽帧管线 + 多状态分发）

## 新功能（2026-07-29 · 视频序列帧动画皮肤）

- **背景**：曾计划"伪 Live2D 分层绑定"（切片精灵 + 部件驱动，已出完整方案并开工），用户提出更优思路后废弃回滚——**AI 生成视频抽帧**，动作连贯性由视频保证，零手工绑定。Live2D/Spine SDK 均与 MIT 公开仓库冲突（Spine 运行时需每份分发对应付费许可；Live2D 模型再分发受限），视频抽帧零授权风险。
- **管线 `tools/video_frames.py`**（opencv+Pillow+numpy+scipy，离线一次性）：视频 → 按目标 fps 抽帧 → 逐帧去白底（复用 `cutout.remove_white_bg`，已从其抽出，CLI 不变）→ **只保留最大连通域** → 全帧并集包围盒统一裁剪（防抖动）→ 降采样 → **循环段检测**（全枚举 (i,j) 首尾 MSE ≤ 150 的最长段，失败回退 ping-pong）→ 网格精灵表 `<状态>.png` + 清单 `<状态>.json` + `_poster.png` + `_preview.png`（肉眼验收）。`--start/--end` 裁段、`--colors 256` 调色板量化（默认开，平涂卡通近乎无损、**体积减 ~85%**）。
- **素材排障（豆包生成视频的坑）**：① 水印是**半透明白字**——白底上隐形、去背后成不透明白斑，且整行文字连通成超大分量（达角色 10%），按比例阈值杀不掉，只能取最大连通域；水印位置随帧漂移，遮角也遮不住。② 首个 idle 视频有**缓慢镜头推近**（zoom），全长无循环段；用 `--start/--end` 截取稳定段解决。③ 循环 MSE 阈值经验值 150（本素材相邻帧中位 ~258，候选段 66~141）。④ 第二批视频（用户重制）无水印无 zoom，质量显著更好。
- **产出（5 状态精灵表，共 ~1.6MB）**：`src/assets/frames/green-anim/{idle,thinking,tool-use,error,success}.png|json`——idle 60帧 loop（摇晃+眨眼）、thinking 11帧 loop（托腮）、tool-use 80帧 ping-pong（齿轮特效，`--start 1.2 --end 4.6 --fps 10` 裁中立段）、error 18帧 loop（!!+汗滴）、success 19帧 loop（比耶欢呼）。`--colors` 量化后 14MB→1.6MB。
- **前端 `src/lib/frame-player.ts`**：`frameIndex`（纯函数：loop 取模 / pingpong 三角波，5 个单测）+ `FramePlayer`（精灵表 drawImage 源矩形取帧，高 150px 等比 + 阴影，与静态精灵一致）。
- **多状态分发（renderer.ts）**：`FRAME_SKINS` = 皮肤 → 动画名 → 精灵表；`drawCharacter` 取当前动画专属序列、缺省回退 idle 序列；**有专属序列时跳过整体参数变换**（动作由序列承担），回退时整体变换照常（waiting/streaming = idle 帧 + bounce/bob，立即可用）。序列随动画切换从头播放（`now - animStart`）。
- **集成（最小增量，未做注册表重构）**：`Config.svelte` SKINS 加一条（poster 缩略图）；`lib.rs` build_menu 加第三个 CheckMenuItem。新皮肤 key：`green-anim`（绿毛衣·动画）。
- **视频需求清单**：`data/Character Vedio/视频需求.md`——5 状态已接入，waiting/streaming 待制作（含拍摄要求与接入命令）。
- **验证**：`tsc` 通过、`cargo check` 通过、`npm test` 16+5 全绿、`npm run build` 通过（4 精灵表 ~1.5MB 入包）。
- **待用户实测**：换"绿毛衣·动画"皮肤，真实任务走 idle→thinking→tool-use→completed 流转；ping-pong 的 tool-use 齿轮出现/消退是否自然。

## 历史状态：Phase 8 · 层 A 工程基建完成（版本对齐 / 最小测试 / AGENTS+License / 文档标注）

| Phase | 内容 | 状态 |
|-------|------|------|
| 1 | Tauri 透明置顶窗口 + 托盘 + 拖拽 + 位置记忆 | ✅ 完成 |
| 2 | Canvas 变换式动画引擎 + 状态机 + 模拟事件联调 | ✅ 完成 |
| 3 | 本地 HTTP POST 事件通道 + 前后端事件桥 + mock-adapter | ✅ 完成 |
| 4 | Claude Code 适配器 + 端到端打通（里程碑 M2） | ✅ 完成 |
| 5 | Cursor / Codex 适配器（多 Agent 通用） | ✅ 完成 |
| 6 | 官方精灵（换肤）+ 右键菜单 + 配置面板 | ✅ 完成 |
| 7 | Win 打包 + 开机自启 + 自动更新 | ✅ 完成 |
| 8A | 工程基建：版本对齐、最小测试、AGENTS.md、LICENSE、设计文档标注 | ✅ 完成 |

## 已完成（Phase 8A · 工程基建）

- **版本对齐**：`package.json` / `tauri.conf.json` / `Cargo.toml` 均为 **0.1.1**；发版说明改为三处同步 bump。
- **最小测试**：
  - 前端 vitest：`state-machine` 聚合优先级、`petState` 过滤/衰减/终态消散
  - 适配器 `node:test`：`adapters/lib/event-map.mjs` MAP 快照与核心映射（从 pet-bridge 抽出共享模块）
  - Rust：`EventRouter` 去重、空字段拒绝、会话标签、filter
  - 一键：`npm test`
- **AGENTS.md + MIT LICENSE**：扩展点、零阻塞约束、文档真相源、发版与测试期望。
- **设计文档**：`开发计划文档-v2.md` 文首增加已实现/废弃/待做表；文末状态改为 v2.2。
- **真机验收（仍待用户）**：Codex CLI / Cursor hooks 端到端、Phase 6 皮肤/菜单/设置实测——基建不代替真机，清单见下方「待用户验证」。

### 待用户验证（非阻塞下一阶段开发）
1. Claude Code：真实任务走完 idle→thinking→tool-use→completed
2. Codex CLI / Cursor：装 hooks 后操作一次；不符时用 `PET_DEBUG=1` 校准
3. 换肤 / 设置热更 / 终态点击消散 / 重启保留

## 已完成（Phase 7 · 打包发布）

### 7.0 隐藏窗口边框
- 现象：透明窗周围一圈 Windows DWM 窗口阴影/细边框。
- 修复：main 窗口 `"shadow": false`（一行配置）。

### 7.1-7.2 仓库化 + 签名密钥
- git init + 首提交；**GitHub 仓库 [shuxiayouzhimao/codex-DP](https://github.com/shuxiayouzhimao/codex-DP)（public——updater 匿名拉取的前提）**。
- `tauri signer generate` 密钥对：私钥 `~/.tauri/codex-pet.key`（空密码，不入库！）；公钥写入 tauri.conf `plugins.updater.pubkey`；私钥+密码存入 repo secrets。
- **坑**：本地 `tauri build` 必须显式 `TAURI_SIGNING_PRIVATE_KEY_PASSWORD=""`，否则签名步骤等在 stdin 密码输入上假死。

### 7.3 打包配置
- `bundle.targets: ["nsis"]`、`createUpdaterArtifacts: true`、`windows.nsis: {languages: [SimpChinese], installMode: currentUser}`（免管理员）。
- **坑**：NSIS 配置在 `bundle.windows.nsis`（不是 `bundle.nsis`）；installMode 枚举是 `currentUser`（不是 perUser）。
- 产物：`Codex Pet_X.Y.Z_x64-setup.exe` + `.sig`（updater 用，NSIS 直接签 exe 不打 zip）。

### 7.4 开机自启（tauri-plugin-autostart）
- `Settings.autostart`（默认 true，serde `#[serde(default)]` 兼容旧 settings.json）；setup 时同步注册表 Run 键；菜单「开机自启」勾选项 + 配置面板开关；`set_settings` 统一同步。

### 7.5 自动更新（tauri-plugin-updater + process）
- 端点：`releases/latest/download/latest.json`；启动时前端 `checkForUpdates()` 静默检查 → downloadAndInstall → relaunch；所有错误吞掉（无 release 时 404 正常）。

### 7.6 CI（.github/workflows/release.yml）
- tag `v*` 触发 windows-latest → tauri-action@v0：构建 + 签名 + draft release + 上传安装包/签名/latest.json。
- **坑**：job 必须显式 `permissions: contents: write`，否则 GITHUB_TOKEN 建 release 报 "Resource not accessible by integration"。
- 发版：bump 版本（tauri.conf.json + Cargo.toml）→ `git tag vX.Y.Z` → push → publish draft。

### 自动更新闭环验证（v0.1.0 → v0.1.1，真实通过）
- v0.1.0 本地安装 → 发布 v0.1.1 → 重启已装宠物 → 启动静默检查 latest.json → 下载 + 验签 → NSIS `/UPDATE` 模式（跑旧卸载器→装新版→自启新进程）→ 注册表 DisplayVersion 与 exe 版本号均变为 **0.1.1**。✅
- **迷惑点**：NSIS 解包保留打包文件的原始 mtime（= CI 构建时间），所以更新后 app.exe 的修改时间看起来"没变"——判断更新与否要看版本号/卸载注册表，不能看 mtime。
- 更新流程耗时 ~1-2 分钟（下载 3.6MB + 卸载重装），期间宠物会消失再出现（正常）。

### 本地验证（v0.1.0）
- NSIS 静默安装 `/S` 成功（装到 `D:\Users\20431\AppData\Local\Codex Pet\`——**注意本机用户目录重定向在 D 盘**）；开始菜单快捷方式创建；安装后运行事件通道正常；Run 键自动从 dev 时的 debug 路径改写为安装路径。
- 顺带修复：配置窗口 X 改为隐藏不销毁（解决"设置无法再打开"）；窗口位置记忆只跟踪 main（避免拖配置窗覆盖宠物位置）。
- **单实例注意**：4271 端口冲突时后启动的实例会打印"绑定失败"但窗口照常（事件通道不可用）——dev 与安装版不要同时跑。

## 已完成（Phase 6 · 体验打磨：官方精灵 + 右键菜单 + 配置面板）

### 6.1 抠图管线（离线一次性）
- `tools/cutout.py`（Pillow + numpy + scipy，不加项目依赖）：① 右下角"豆包AI生成"水印矩形填白；② 近白掩码 → `scipy.ndimage.label` 连通域 → **只删与图像边缘连通的近白区域**（白衣白裙被描边包围、不与边缘连通，不会打穿）；③ 背景 alpha=0 → 内容包围盒裁剪 + 8px 边距 → 降采样 ≤512px LANCZOS → RGBA PNG。
- 产出：`src/assets/sprites/sprite-green.png`（389×512，绿毛衣眼镜）+ `sprite-red.png`（367×512，红装传统）。已验证：四角 alpha=0、无水印、无白边、红装白衣完整无破洞。
- 用法：`python tools/cutout.py <源图> <输出.png> [容差=30]`，可复用于未来新皮肤。

### 6.2 渲染器接精灵 + 换肤
- `renderer.ts`：Vite 静态导入两款精灵；`loadSkins()` 预加载（失败皮肤缺失时**回退程序化 blob**，健壮）；`setSkin(name)` 切换。`drawCharacter` 拆为：精灵路径（程序化阴影 + `drawImage` 原点居中、高 150px 等比）/ blob 兜底路径；阴影抽为 `drawShadow`。**变换栈/rAF/`setAnim` 一概未动**（idle 呼吸、thinking 倾斜、success 弹跳等照常作用于精灵）。
- `imageSmoothingQuality = "high"` 保证 512→150 缩放质量。

### 6.3 设置后端 + 宠物右键菜单
- **共享设置（Rust 拥有）**：`Settings{skin, clickToDismiss, terminalHoldMs, workDecayMs}`（camelCase），持久化 `app_config_dir/settings.json`（仿 window-state.json）；启动时载入。
- **命令**（`core:default` 无需新权限）：`get_settings` / `set_settings`（落盘 + `emit("settings-changed")` 广播 + 刷新菜单）/ `show_pet_menu`（`run_on_main_thread` 里 `build_menu` + `menu.popup(window)`）。
- **菜单事件收口到应用级**：`Builder.on_menu_event` 统一分发，托盘菜单与宠物右键菜单**共用同一 `build_menu`**（id 一致）；托盘 builder 不再单挂 handler。
- **菜单扩展**：换肤子菜单（绿毛衣/红装，单选勾选）→ 改设置广播即时生效；「设置…」→ 显示并聚焦 config 窗口。
- 前端：`.pet` 加 `oncontextmenu` → `invoke("show_pet_menu")`（与左键点击/拖拽零冲突）；启动 `get_settings` 初始化 + `listen("settings-changed")` 热更 → `renderer.setSkin` + `petState.setOptions`。
- 修复两处编译错：`Menu::popup` 按值接收 `Window`（不传引用）；`run_on_main_thread` 闭包再遇 E0505 → 克隆独立 handle。

### 6.4 配置面板（第二个窗口）
- `tauri.conf.json` 静态声明 `config` 窗口（440×560、有装饰、非置顶、`visible:false`、`url:/config.html`）——静态声明避免 `core:window:allow-create` 权限。
- Vite 多入口：`rollupOptions.input` 加 `config.html`；新增 `config.html` + `src/config/{main.ts,Config.svelte}`。
- 面板 UI：皮肤单选（带精灵缩略图）、终态提醒（点击确认 / 5s 自动消失 + 兜底时长档位 10/30/60min）、工作态衰减档位（1/5/15min）。改动即 `set_settings`；同时监听 `settings-changed` 回显外部改动（如右键菜单换肤）。
- `petState.setOptions(partial)`：常量改为可变选项，`timeoutFor(s)` 分类计时；`clickToDismiss=false` 时终态 5s 自动回闲置。气泡"· 点击消散"提示仅在 clickToDismiss=true 时显示。
- capabilities `windows` 加 `"config"`。

### 验证
- `tsc` 通过、`cargo check` 通过、`npm run build` 通过（双入口 + 精灵打包，main 22.9kB / config 7.8kB JS）。
- dev 重启后：事件链路正常（mock `completed` → `200 ok` → FWD），真实 Claude Code 会话事件照常到达。
- **待用户实测**：宠物显示绿毛衣形象；右键弹原生菜单；换肤即时生效；「设置…」打开面板、改动即时同步主宠物、重启后保留。

## 已完成（Phase 1）
- Tauri 2 + Svelte 5 + Vite 6 工程搭建（根目录，手工非脚手架）
- 透明 / 无边框 / 置顶 / 不占任务栏窗口
- 系统托盘：显示 / 隐藏 / 退出；左键单击切换显隐
- 整窗拖拽移动；位置 400ms 防抖落盘 `app_config_dir/window-state.json`，启动恢复
- 实测常驻内存 ~37MB（远轻于 Electron 的 150MB+）

## 已完成（Phase 2）
- Canvas 变换式动画引擎 `src/lib/renderer.ts`：bob/bounce/tilt/breath/shake/jump 参数化变换，`requestAnimationFrame` 循环，DPR 适配。
- 动画配置 `src/assets/config/animations.json`（7 种动画）+ 状态机 `src/lib/state-machine.ts`（8 个 Agent 状态 → 动画/文案/优先级映射 + 多会话聚合）。
- 程序化占位角色（透明底 kawaii blob，含眨眼/表情随状态变化），引擎支持后续无缝换成真实抠图精灵。
- once 动画（success/error）播完自动回 idle 并回调同步文案。
- DEV 控制面板（8 状态按钮 + 自动演示开关，默认每 2.2s 轮播），Phase 3 接真实事件后移除、窗口改回 200x200。

## 已完成（Phase 3）
- 本地事件推送通道 `src-tauri/src/server.rs`：`tiny_http` 监听 `127.0.0.1:4271/event`，接收适配器 POST 的统一事件 JSON。
- 事件路由/去重 `src-tauri/src/state_machine.rs`：按 `source:sessionId` 去重（状态未变返回 `dup` 不转发），`app.emit("agent-event")` 到前端。
- 前端事件桥 `src/lib/events.ts`：监听 `agent-event`，维护多会话 Map + 聚合（最高优先级）→ 驱动动画与气泡文案（优先显示 `detail`）。
- mock-adapter `adapters/mock/push.mjs`（零依赖 Node，内置 fetch）：`--state X` 单推 / `--demo` 循环。
- 端到端验证：thinking/tool-use/completed → `200 ok`，重复 completed → `200 dup`，应用无崩溃。
- 移除 DEV 面板，窗口改回 200x200。

## 已完成（Phase 4 · 里程碑 M2）
- Claude Code 适配器 `adapters/claude-code/pet-bridge.mjs`（零依赖 Node）：读 hook stdin JSON → 按 `hook_event_name` 映射统一事件 → POST /event。永远 exit(0)、1s 超时、静默失败，绝不阻塞 Claude Code。
- 安装器 `adapters/claude-code/install-hooks.mjs`：备份 + 幂等合并 `~/.claude/settings.json`，`--uninstall` 可卸载（只移除本桥接器）。
- **重要发现**：本机已装另一款桌宠 **Vibe Pet**，其 hooks 提供了真实事件名全集，据此采用更精确映射：`PermissionRequest`→等待审批、`PostToolUseFailure`/`StopFailure`→出错、`Elicitation`→需确认、`SubagentStart`→执行中等 15 个事件。
- 注册采用**追加**：Vibe Pet hooks 全部保留（pet-bridge 15 处 + Vibe Pet 16 处共存，JSON 校验有效）。两款桌宠哪个在运行就哪个响应；若只想留 codex-DP，退出 Vibe Pet 应用即可。
- 验证：pet-bridge 对 15 种事件的映射全部正确且 POST 200。

## 已完成（Phase 5 · 多 Agent 通用：Codex / Cursor 适配器）
- **关键调研**：先用 WebSearch 核实外部工具真实事件源（WebFetch 被网络策略封锁）。**本机实证**（读 Vibe Pet 留下的 hooks.json）拿到权威格式：
  - **Codex CLI 0.144.4**：`~/.codex/hooks.json`，结构与 Claude Code **完全同构**（`hooks.<Event>.[].hooks[].{type,command,timeout}`），事件名 PascalCase；需 `config.toml` 有 `[features] hooks = true`（已存在）。比旧 `notify` 机制（仅 turn-complete）丰富。
  - **Cursor 1.7+**：`~/.cursor/hooks.json`，扁平结构（`hooks.<event>.[].command` + `version:1`），事件名 camelCase。**推翻旧记忆"Cursor hooks 虚构"**（那条针对的是 v1 文档里的具体假想格式）。
- **统一桥 `adapters/claude-code/pet-bridge.mjs` 推广为多 Agent**：新增 `--source <claude-code|codex|cursor>`；事件映射兼容 PascalCase（Claude/Codex）与 camelCase（Cursor）两套命名；**防御式字段提取**（session_id/conversation_id、tool_name/toolName、cwd/workspace_roots）；`PET_DEBUG=1` 原始载荷日志到 `~/.codex-dp-hook-debug.jsonl`（校准真实 schema 用）。向后兼容，现有 Claude Code hooks 无需改动。
- **两个安装器**（幂等、先备份、`--uninstall` 可卸）：`adapters/codex/install-hooks.mjs`（嵌套格式 + 确保特性开关）、`adapters/cursor/install-hooks.mjs`（扁平格式 + `version:1`）。均**顺带清掉 Vibe Pet 死 hooks**（Codex 6 条、Cursor 11 条）。
- **测试**：桥对 codex/cursor/claude-code 三源映射全部 `200`，后端 FWD 日志 source/session/detail 正确（含 toolName 防御提取）；两安装器写出的 hooks.json 结构与已证实格式一致。
- **待真实验证**：需用户在 Codex CLI / Cursor 里实际操作一次，确认 hooks 真实触发驱动桌宠；若字段名不符，开 `PET_DEBUG=1` 抓真实载荷再校准。

## 修复记录（2026-07-28 · 实时体验）
- **现象**：实时时大部分卡闲置、思考一闪即回闲置、完成不显示；另发现本机已装 Vibe Pet。
- **处理**：① 删除 Vibe Pet hooks（16 条，备份 `settings.json.vibepet-removal.bak`）；② 给事件 server 加日志，确认链路本身正常——当前会话已热加载 hooks，事件流/映射/会话 id 全部正确（`tool-use/thinking` 实时转发）。
- **根因**：在前端状态显示，而非事件缺失——`completed`/`error` 是 once 动画，仅 700ms 即回 idle，肉眼看不清；造成"思考直接掉闲置、没有完成态"的错觉。另有隐藏 bug：前端会话 Map 从不过期，污染聚合。
- **修复**：重构前端状态管理 `src/lib/petState.ts`——完成/出错**保持 3.5s 可见**再回闲置；工作状态 **45s 无事件才衰减**（不再快速掉闲置）；会话 **90s 过期**。`success`/`error` 动画改为循环。前端经 vite HMR 热重载生效，无需重启。

## 修复记录（2026-07-28 · 拖拽 + 监听范围）
- **拖拽失效**：根因是 Phase 2 把 Phase 1 的 `startDragging()` 换成了 `data-tauri-drag-region` 属性；该属性依赖 Tauri 注入脚本，而配置 `"csp": null` 影响注入导致失效。**改回显式 `getCurrentWindow().startDragging()`**（权限 `allow-start-dragging` 已在 capabilities），直接调 API 不依赖注入，可靠。
- **监听范围可见化**：确认 hooks 注册在**用户级** `~/.claude/settings.json` → 对本机**所有** Claude Code 会话生效（非单个对话）。`petState` 新增 `SessionMeta`（活跃会话数/来源/最近会话 key），前端底部加**会话指示器**（如 `claude-code·dbaf7e`，多会话时 `+N`），鼠标悬停有说明。`tsc` 通过、vite HMR 已生效。

## 新功能（2026-07-28 · 选择监听某个会话）
- **需求**：可选择只监听某一个对话，而非聚合全部。
- **用法**：托盘右键 → **监听会话** 子菜单 → 「全部会话」或某个具体会话（单选勾选）。选中后桌宠只反映该对话；选「全部会话」恢复聚合。
- **会话识别**：适配器从 hook 载荷 `cwd` 提取**项目文件夹名**转发为 `project` 字段，菜单/指示器据此显示 `项目名·会话短id`（如 `codex-DP·dbaf7e`），不再是难以辨认的裸 UUID。同项目多对话靠短 id 区分。
- **实现**（全栈）：
  - `pet-bridge.mjs`：`projectOf(cwd)` 取基名 → `project` 字段。
  - `state_machine.rs`：`AgentEvent` + `project`；`EventRouter` 新增**会话跟踪**（90s TTL，无论是否去重都刷新活跃时间）+ **filter** + `list_sessions()`（剔除过期、按最近活跃排序、生成标签）。
  - `lib.rs`：`AppState` 增加 `router`（共享）+ `tray`（句柄，供刷新）；`build_menu()` 动态生成「监听会话」子菜单（CheckMenuItem 单选）；`refresh_menu()` 经 `run_on_main_thread` 在主线程 `set_menu`；`on_menu_event` 处理 `sess:*` 选择 → `set_filter` + `emit("session-filter")` + 刷新。
  - `server.rs`：改用 `AppState` 里的共享 router；每个被转发的事件后 `refresh_menu()`（保持会话列表新鲜）。
  - 前端：`events.ts` + `listenSessionFilter`；`petState.setFilter(key|null)`（切换时清空会话视图）；指示器过滤模式显示 🎯 前缀，目标空闲时也常显。
- **设计取舍**：① 过滤**不做持久化**——sessionId 每个对话都是新 UUID，重启后旧 id 无意义，故每次启动默认「全部会话」（刻意的正确行为）。② 过滤在**前端**生效，后端始终跟踪所有会话以维持菜单完整。③ 只能选 90s 内**活跃**的会话；不活跃的对话产生事件后才会出现。
- **验证**：`tsc` 通过；`cargo check` 通过（修复 `set_menu` 需 `Option`、`run_on_main_thread` 闭包借用冲突两处编译错误）；node 推送 3 条带 project 的事件全部 `200 ok`，菜单主线程刷新无崩溃。

## 故障记录（2026-07-28 · 突然"看不到当前会话/收不到事件"）
- **现象**：托盘「监听会话」里看不到当前会话，桌宠也不响应。
- **排查**：服务器正常（无 4271 绑定失败，手动 POST `200 ok`）；`pet-bridge.mjs` 手动管道测试也 `200`；但重启后当前会话 `dbaf7e9c` 再无事件。
- **根因**：`~/.claude/settings.json` 的 `hooks` 段**只剩 Vibe Pet 的 hooks，pet-bridge 的 15 条全没了**。即 **Vibe Pet 这个已安装应用会整体重写 `settings.json` 的 `hooks` 段，把 pet-bridge 条目顶掉**（之前删它的 hooks 也被它自己加回来了）。于是 Claude Code 不再为会话调用 pet-bridge → 桌宠无事件 → 会话不进菜单。
- **修复**：重跑 `node adapters/claude-code/install-hooks.mjs`（幂等合并，保留 Vibe Pet/codegraph，先备份）。15 条 pet-bridge 写回后事件立即恢复（`执行 Grep` 等实时到达）。
- **⚠️ 会复发**：只要 Vibe Pet 仍在运行/自启，它还会再次顶掉 pet-bridge hooks。**根治需退出（最好卸载或禁自启）Vibe Pet**；否则需不时重跑 install-hooks.mjs。

## 处置（2026-07-28 · Vibe Pet 彻底清除，已授权）
- **侦查结论**：Vibe Pet **其实已卸载**——注册表 HKLM/HKCU Uninstall 无、`D:\Program Files\Vibe Pet` 目录不存在、无进程、无任何自启（Run 键/启动文件夹/计划任务全无）。真相是：它当天 ~15:19 曾运行并重写 hooks（顶掉 pet-bridge），之后被卸载，留下孤儿残留。不存在"卸载/禁自启"的对象（已没了）。
- **清理残留**（用户授权"禁用或卸载"）：① `remove-vibepet-hooks.mjs` 移除死 Vibe Pet hook 16 条（备份 `settings.json.vibepet-removal.bak`）；② 孤儿目录 `~/.code-pet` 改名为 `~/.code-pet.vibepet-purged`（可逆中和，可硬删）。
- **校验**：settings.json 现仅 pet-bridge(15) + codegraph(2)，Vibe Pet/hook-runner 归零；当前会话事件实时通畅（`执行 Bash/Grep` 等到达）。Vibe Pet 已无任何残留能再干扰 codex-DP。

## 改进（2026-07-28 · 状态持久化重做：思考不掉线 / 完成需点击确认）
- **痛点**：① 思考/等待时桌宠却显示闲置（慢生成如代理时单次超 45s 无事件就掉线）；② 完成仅 3.5s 即回闲置，起不到提醒作用。
- **重做 `petState` 持久化模型**（按状态分类计时，新事件随时打断）：
  - 工作态 thinking/tool-use/streaming：安全网衰减 45s → **5min**（慢生成不再中途掉闲置）。
  - 等待态 permission-prompt/ask-user：**不衰减**，直到用户操作引发新事件（30min 兜底）——不再错过"它在等你"。
  - 终态 completed/error-interrupted：**保持到用户点击桌宠确认**（`dismissTerminal`）或新事件（30min 兜底）——真正起提醒作用。
- **点击 vs 拖拽**：`App.svelte` 重写 pointer 交互——按下不立即拖拽，移动超 6px 才 `startDragging()`（保留 OS 原生拖拽、自动处理 DPI）；无移动则为**点击** → 确认终态回闲置。终态气泡加"· 点击消散"提示 + 高亮（完成绿/出错红）。
- 纯前端改动，vite HMR 热更新生效；`tsc` 通过。

## 传输修正（相对 v2 文档）
- 原计划 **WebSocket** → 实际 **HTTP POST**。原因：Claude Code Hooks 每次事件都新建进程，单向 fire-and-forget 推送用无状态 POST 更简单、更健壮（无需重连），适配器可零依赖（curl 一行 / Node fetch）。v2 文档 §1/§2/§3 的 "WebSocket" 表述以此为准。

## 品牌（2026-07-31 · 专属图标）

- 运行时：`src-tauri/icons/*`（安装包/窗口）+ `icons/tray.png`（托盘）+ `src/assets/brand/mark-128.png`（设置页）
- 再生：把母稿放到 `data/Icon/*.png` 后执行 `python tools/make_brand_icons.py` → `npx tauri icon brand/app-icon-1024.png --output src-tauri/icons` → 再跑一遍 `make_brand_icons.py` 写回 tray（中间产物可不入库）

## 调整（2026-07-31 · 会话 TTL + 命名）
- 会话活跃窗口 **90s → 180s**（Rust `SESSION_TTL` + 前端 `petState` 同步；配置页提示与 README 已改）。
- 会话展示名改为 **`来源标签·项目名`**（如 `Cursor·codex-DP`）；同来源同项目多对话按 **最近活跃** 编号，最新无后缀，其后 `_1` `_2`…。无 project 时项目位显示「会话」。副文案/菜单带短 id。

## 体验（2026-07-31 · 会话监听可感知性）
- **指示器**：全部模式显示「刚收到 来源·项目」；锁定无事件「暂无新事件」；失联橙色条可点回全部；锁定时忽略其它源会短暂「已忽略 Cursor」。
- **滞回**：全部模式下工作态同级（thinking）不抢戏；更高优先级 / 终态/等待可抢；当前终态不被其它会话 thinking 盖住。
- **去重保活**：指纹未变仍刷新 Rust `last_seen`，并发 `session-alive` 刷新前端会话 TTL（不改动画）。
- **失联出口**：设置页横幅 + 托盘「…·已失联」项 → 回全部（不做自动跟新 UUID）。

## 关键决策
- **数据源**：Push 模型 —— 桌宠内置本地 HTTP POST server（`/event`），各 Agent 适配器推统一事件。
- **目标**：多 Agent 通用，Claude Code 为主（真实 Hooks），Cursor/Codex 为适配器。
- **精灵素材**：`data/` 下图片**带白底，不能直接用作精灵，仅供测试**；正式精灵需透明抠图。Phase 2 先用程序化占位角色（透明底），引擎支持后续无缝替换。

## 运行方式
- 开发：`npm run tauri dev`（Vite :1420）
- 前端构建：`npm run build`；Rust 检查：`cargo check`（src-tauri/）
- 坑：npm `allow-scripts` 会拦 esbuild postinstall → 需 `node node_modules/esbuild/install.js`；1420 端口被占会杀死 dev 启动。
